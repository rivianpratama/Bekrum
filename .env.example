UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# TURN server (Metered.ca) — required for peers behind symmetric NATs / firewalls
METERED_APP_NAME=
METERED_API_KEY=

# Optional: static ICE servers override (JSON array). Used only when Metered vars are absent.
ICE_SERVERS_JSON=[{"urls":"stun:stun.l.google.com:19302"}]
